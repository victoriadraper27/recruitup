
<p><%= simple_form_for(@schedule_event) do |f| %>
  <%= f.input :user_id, collection: team.users, input_html: { class: 'select2' }, class: 'form-inputs' %>
  <%= f.submit "Assign Event", class: "btn" %>
  <% end %>
</p>
